import React from 'react';
import { Routes, Route } from 'react-router-dom';

// Bạn kia sẽ import các trang HomePage, CartPage, CheckoutPage... vào đây

export const ClientRoutes = () => {
  return (
    <Routes>
      {/* M ĐIỀN CODE ROUTER CỦA KHÁCH HÀNG VÀO ĐÂY NHÉ. 
          VD: <Route path="/" element={<HomePage />} /> 
      */}
    </Routes>
  );
};